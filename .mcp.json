{
  "mcpServers": {
    "playwright": {
      "command": "pnpx",
      "args": ["@playwright/mcp@latest", "--browser", "chromium", "--isolated", "--no-sandbox"]
    },
    "convex": {
      "command": "pnpm",
      "args": ["convex", "mcp", "start"]
    }
  }
}
